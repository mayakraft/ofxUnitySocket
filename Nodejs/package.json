{
	"name": "ofxUnitySocket",
	"version": "1.0.0",
	"description": "send data between openFrameworks and Unity",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "MIT",
	"dependencies": {
		"express": "^4.14.0",
		"socket.io": "^1.4.8"
	}
}
